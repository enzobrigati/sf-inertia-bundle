<?php

declare(strict_types=1);

namespace EnzoBrigati\InertiaBundle\Prop;

/**
 * Only included when explicitly requested via partial reloads.
 */
class OptionalProp extends CallbackProp implements PartialLoadPropInterface
{
}
